# Cloud Firewall Plus Module

This module creates a hierarchical firewall policy with an IPS-enabled rule for traffic to the PSC endpoint. TLS inspection can be enabled through additional configuration if required.
